name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: unit-tests
      run: |
        composer install
        vendor/bin/phpunit tests
